# Kubernetes Manifests (Template)

This folder is reserved for Kubernetes manifests to support container orchestration and scaling in production environments.

- `deployment.yaml` — Template for application deployment
- `service.yaml` — Template for exposing services

Update these files as needed for your cluster and environment.
